import 'package:flutter/material.dart';

class ExpiryPage extends StatelessWidget {
  const ExpiryPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Application expired. Please contact with administrator.'),
    );
  }
}